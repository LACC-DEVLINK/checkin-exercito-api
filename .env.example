// Environment Variables - TODO: Variáveis de ambiente
# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/checkin_exercito_db?schema=public"

# Alternative format for individual components (if needed by other services)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=username
DB_PASSWORD=password
DB_NAME=checkin_exercito_db
DB_SCHEMA=public

# Application Configuration
NODE_ENV=development
PORT=3000

# JWT Configuration (for future authentication)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Logging
LOG_LEVEL=info

# App Configuration
APP_NAME="CheckIn Exército API"
APP_VERSION=1.0.0